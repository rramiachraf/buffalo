### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Cart {
  id: ID
  name: String
  poster: String
  price: Float
}

"""
A date string, such as 2007-12-03, compliant with the `full-date` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar Date

enum Device {
  pc
  playstation
  xbox
}

type Game {
  description: String!
  device: Device!
  id: ID!
  name: String!
  platform: Platform
  poster: String
  price: Float!
}

type Mutation {
  addGame(description: String!, device: Device!, name: String!, platform: Platform, price: Float!): Boolean
  addToCart(gameId: ID!): Boolean
  checkout: String
  createUser(email: String!, firstName: String!, lastName: String!, password: String!): User
  login(email: String!, password: String!): User
  logout: Boolean
  removeFromCart(gameId: ID!): Boolean
  updateRole(newRole: Role!, userID: Int!): Boolean
}

type Order {
  amount: Float!
  game: String!
  id: Int!
  key: String!
}

enum Platform {
  blizzard
  origin
  steam
  uplay
}

type Query {
  cart: [Cart]!
  game(id: ID!): Game
  games: [Game]
  getOrders: [Order]
  me: User
  searchGame(query: String!): [Game]
  total: Float
  user(id: Int!): User
}

enum Role {
  admin
  customer
  moderator
}

type User {
  createdAt: Date!
  email: String!
  firstName: String!
  id: Int!
  lastName: String!
  role: Role!
}
